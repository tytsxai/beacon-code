# Example environment for a supervised CLI or app-server instance.

CODE_HOME=/var/lib/code
CODE_SECURE_MODE=1
RUST_LOG=info

# Cleanup retention (tune to your policy).
CODE_CLEANUP_SESSION_RETENTION_DAYS=30
CODE_CLEANUP_WORKTREE_RETENTION_DAYS=7
CODE_CLEANUP_LOG_RETENTION_DAYS=30
CODE_CLEANUP_LOG_MAX_BYTES=52428800
CODE_CLEANUP_LOG_TRUNCATE_MIN_AGE_MINUTES=10
CODE_CLEANUP_MIN_INTERVAL_HOURS=6

# Provider auth (set via your secret store, not inline).
# OPENAI_API_KEY=...
